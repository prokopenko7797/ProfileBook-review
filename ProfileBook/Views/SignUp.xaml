<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ProfileBook.Views.SignUp"
             Title="{Binding Resources[SignUpTitle]}"
             BackgroundColor="{AppThemeBinding Light={StaticResource LightPageBackgroundColor}, Dark={StaticResource DarkPageBackgroundColor}}">

    <StackLayout Padding="30,0,30,0" HorizontalOptions="FillAndExpand"  VerticalOptions="CenterAndExpand" >

        <StackLayout Padding="0,0,0,150">

            <Entry Placeholder = "{Binding Resources[Login]}" 
                   Text="{Binding Login}" 
                   MaxLength="16" />
            <Entry Placeholder = "{Binding Resources[Password]}" 
                   IsPassword ="True" Text="{Binding Password}" 
                   MaxLength="16"/>
            <Entry Placeholder = "{Binding Resources[ConfPas]}" 
                   IsPassword ="True" 
                   Text="{Binding ConfirmPassword}" 
                   MaxLength="16" />
        </StackLayout>
        <Button Text="{Binding Resources[SignInButton]}" BackgroundColor="DarkOrange" Command="{Binding AddUserButtonTapCommand}"></Button>
        

    </StackLayout>

</ContentPage>